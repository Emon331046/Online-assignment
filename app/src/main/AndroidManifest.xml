<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlineassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SingleSubmissionActivity"></activity>
        <activity android:name=".ShowSubmissionsActivity" />
        <activity android:name=".SubmissionActivity" />
        <activity android:name=".SingleImageActivity" />
        <activity android:name=".AssignmentUploadActivity" />
        <activity android:name=".AssignmentActivity" />
        <activity android:name=".StudentLogin" />
        <activity android:name=".TeacherLogin" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>